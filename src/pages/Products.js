import Heading from "../components/layout/Heading";


const Products = () => {
    return (
        <div
        style={{ marginTop: `${20}rem` }}>
        <Heading 
            fontSize={50} 
            title="GALLERY" />
    </div>
    );
};

export default Products;